# Job-level config for pricing_nightly_batch — prod environment.
# See dev.yaml for field documentation.
# CHANGE CONTROL: any change to this file follows the standard PR + review
# process per ci-cd/01-repository-and-branching-strategy.md — never edited
# directly in a running environment.

pricing:
  input_path: "gs://acme-prod-pricing-raw/daily_price_snapshot/"
  zone_lookup_path: "gs://acme-prod-pricing-raw/zone_lookup/"
  output_path: "gs://acme-prod-pricing-curated/daily_price_snapshot/"
  discount_cap_percent: 40
  db_password: "secret://pricing-db-password-prod"

spark:
  shuffle_partitions: 200
